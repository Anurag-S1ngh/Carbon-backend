version: "2"
sql:
  - engine: "postgresql"
    queries: "pkg/db/queries"
    schema: "pkg/db/schema"
    gen:
      go:
        package: "db"
        out: "pkg/db/generated"
        emit_json_tags: true
        emit_interface: true
